= content_for :title, 'Reset your password'

:javascript
  mixpanel.track('View New Password Resets Page');

.uk-container.uk-container-center.uk-margin-large-top.uk-margin-large-bottom
  .uk-width-1-2.uk-container-center
    %h2 Reset your password
    %h4
      %strong This page allows you to request a password reset email.
    %p Please enter the email address you used to create your Beehive account in the 'Email' field below, and click 'Reset password'.
    %p
      More detailed instructions can be found
      = succeed ',' do
        = link_to 'here', faq_path(anchor: 'reset-password'), class: 'faq-link'
      or you can get in touch with
      = link_to 'support', 'mailto:support@beehivegiving.org'
      who'll be happy to help.

    %hr.thick
    .form.uk-form.uk-form-stacked.uk-panel.uk-panel-box.shadow
      = form_tag password_resets_path, method: 'post' do
        .uk-form-row{class: ('field_with_errors' if params[:email_missing])}
          = text_field_tag :email, params[:email], placeholder: 'Email', type: 'email', class: (params[:email_missing] ? 'large uk-width-1-1 field_with_errors' : 'large uk-width-1-1')
          - if params[:email_missing]
            .error-message Please enter the email address you used to create your account
        .uk-form-row
          = button_tag 'Reset password', class: 'uk-width-1-1 uk-button uk-button-primary uk-button-large'
