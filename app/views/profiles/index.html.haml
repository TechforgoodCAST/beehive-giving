:javascript
  mixpanel.track("View Profiles Index Page");

#bg-color
  .uk-container.uk-container-center{:style => "min-height: 480px;"}
    .uk-grid.uk-margin-.uk-margin-top
      %h2.uk-margin-top Your profiles
    .uk-width-1-1.uk-margin
      - if @recipient.can_create_profile?
        = link_to 'New profile', new_recipient_profile_path(current_user.organisation), class: 'uk-button uk-button-success'
    - if @profiles.any?
      .uk-grid
        - @profiles.each do |profile|
          .uk-width-medium-1-3
            .uk-panel.uk-panel-box.uk-margin-bottom
              .uk-width-medium-1-1
                = link_to "Edit #{profile.year}", edit_recipient_profile_path(current_user.organisation, profile), class: 'uk-button uk-button-primary uk-width-1-1'
    - else
      .uk-alert.uk-alert-warning
        %o In order to receive funder recommendations you need to complete your profile. We'll use the information you provide to construct a unique digital fingerprint for your organisation, enabling us to give you personalised funding recommendations.
        = link_to 'Click here', new_recipient_profile_path(@recipient)
        to create a profile.
