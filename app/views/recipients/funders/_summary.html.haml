.uk-grid
  .uk-width-medium-1-3
    -# TODO: performance & cache
    = render partial: 'recipients/funders/card', locals: { fund: fund }

  .uk-width-medium-2-3
    %h3.uk-margin-top
      = link_to fund.name, fund_path(fund), class: 'yellow'
      %span.year.muted= fund.funder.name

    -# TODO: performance & cache
    = render partial: 'recipients/funders/insights', locals: {fund: fund}

    = render_tag_list(fund)
