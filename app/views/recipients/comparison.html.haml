#bg-color
  .uk-container.uk-container-center
    .uk-grid
      = render partial: 'recipients/sections/header'

      .uk-width-medium-1-1.uk-width-large-1-4.uk-vertical-align.uk-text-center{style: 'height: 220px;'}
        .uk-vertical-align-middle
          .uk-panel.cta
            - if @funder.restrictions.any?
              - if @recipient.questions_remaining?(@funder)
                %p
                  First things first. Let's see if you're eligible?
                = link_to 'Are you eligible?', recipient_eligibility_path(@funder), class: 'uk-width-1-1 uk-button uk-button-primary uk-button-large'
              - else
                - if @recipient.eligible?(@funder)
                  %p
                    You're eligible for
                    = succeed ',' do
                      = @funder.name
                    now follow the link below to proceed.
                  %a.uk-width-1-1.uk-button.uk-button-success.uk-button-large{href: '', "data-uk-modal" => "{target:'#eligible'}"}
                    %i.uk-icon-check
                      Eligible
                - else
                  %p
                    Drat! It looks like you're ineligible for
                    = @funder.name
                    why not try
                    = succeed '.' do
                      = link_to 'another funder', funders_path
                  %button.uk-button.uk-button-large.uk-width-1-1{:disabled => "", style: "background: #d32c46; color: #fff;"}
                    %i.uk-icon-ban
                      Ineligible
            - else
              %p What would you like to do next?
              %a.uk-button.uk-button-large.uk-button-primary.uk-width-1-1{"data-uk-modal" => "{target:'#next'}"} Next step

- if @recipient.eligible?(@funder)
  #bg-color
    .uk-container.uk-container-center
      .uk-width-1-1
        .uk-grid
          .uk-width-medium-1-2.uk-margin{style: 'position: relative;'}
            %a.uk-button.uk-button-primary.uk-width-1-1{href: '', "data-uk-modal" => "{target:'#eligible'}"} Approach for funding
            .or or
          .uk-visible-small.uk-width-1-1.uk-text-center.uk-margin-bottom{style: 'font-weight: bold; font-size: 16px; line-height: 30px;'} or
          .uk-width-medium-1-2.uk-margin-bottom
            %a.uk-button.uk-button-primary.uk-width-1-1{:href => "#similar", "data-uk-smooth-scroll" => "{offset: 92}", style: 'margin-bottom: 3px;'} Find another funder

= render partial: 'recipients/sections/overview'

= render partial: 'recipients/sections/funded'

= render partial: 'recipients/sections/similar'

- if @recipient.eligible?(@funder)
  = render partial: 'recipients/eligible'

#next.uk-modal
  .uk-modal-dialog
    %a.uk-modal-close.uk-close
    :plain
      <style>.embed-container { position: relative; padding-bottom: 95%; height: 0; overflow: hidden; max-width: 100%; } .embed-container iframe, .embed-container object, .embed-container embed { position: absolute; top: 0; left: 0; width: 100%; height: 100%; }</style><div class='embed-container'><iframe src="https://docs.google.com/forms/d/1AVr3pcIvkz-LYZbtpUyOCh52uxQNiOw5YO0QaHcwTS0/viewform?embedded=true" width="760" height="500" frameborder="0" marginheight="0" marginwidth="0">Loading...</iframe></div>
