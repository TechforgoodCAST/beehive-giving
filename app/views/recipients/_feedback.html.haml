#feedback.uk-modal{class: ('feedback-prompt' if current_user.sign_in_count > 0 && cookies['_BHfeedbackClose'].blank?)}
  .uk-modal-dialog
    %a.uk-modal-close.uk-close.js-record-feedback-close
    .uk-modal-header
      %h2 Let us know what you think
    = render partial: 'feedback/form'
