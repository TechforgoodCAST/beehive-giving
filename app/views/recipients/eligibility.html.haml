:javascript
  mixpanel.track("View New Eligibility Page", {"Funder": "#{@funder.name}"});

#bg-color{style: 'border-bottom: solid 3px #EAEAEA;'}
  .uk-container.uk-container-center
    .uk-grid
      = render partial: 'recipients/sections/header'

      .uk-width-medium-1-1.uk-width-large-1-4.uk-vertical-align.uk-text-center{style: 'height: 220px;'}
        .uk-vertical-align-middle
          %a.uk-button.uk-button-large{href: recipient_comparison_path(@funder)}
            %i.uk-icon-arrow-left
              Back to funder

#bg-color{style: 'background: #454545'}
  .uk-container.uk-container-center.uk-animation-fade
    .uk-margin-large-top.uk-margin-large-bottom
      %h2.uk-text-center.upcase.white Are you eligible for funding?
      .uk-width-medium-3-4.uk-text-center.uk-container-center
        %p.uk-text-large.white We've worked with the funder to capture their specific crteria below. Simply answer the questions below to see if you are eligible for their support.
      .uk-panel.uk-panel-box#panel
        .uk-form.uk-form-stacked
          = simple_form_for @recipient,:url => recipient_eligibility_path(@funder) do |f|
            = f.simple_fields_for :eligibilities, @eligibility do |e|
              - if e.object.new_record?
                .uk-width-1-1.uk-margin-bottom
                  %label.uk-form-label
                    Are you seeking funding for
                    = succeed '?' do
                      = raw(Restriction.find(e.object.restriction_id).details)
                  = e.hidden_field :recipient_id, value: @recipient.id
                  = e.hidden_field :restriction_id
                  = e.input :eligible, collection: @recipient.restriction_truthy(e.object.restriction), input_html: { class: 'uk-width-1-1' }
                  = e.error :eligible, class: 'uk-text-small uk-text-warning'
            = f.button :button, "Check eligibility", class: 'uk-button uk-button-primary uk-width-1-1', data: {disable_with: "<i class='uk-icon uk-icon-circle-o-notch uk-icon-spin'></i> Checking..."}
