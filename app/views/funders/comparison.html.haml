.uk-container.uk-container-center.uk-margin
  %p In 2014...
  #multiple_funding_frequency_distribution{ data: {data: multiple_funding_frequency_distribution(@funders).to_json} }
  .uk-panel#panel.uk-margin
    .uk-overflow-container
      %table.uk-table.uk-table-hover.uk-table-striped
        %thead
          %tr
            %th{style: 'width: 100px'} Funder
            %th Funded
            %th Approval months
        %tbody
          - @funders.each do |funder|
            %tr
              %td= image_tag "logos/#{funder.slug}-logo.png", alt: "#{funder.name}"
              %td= funder.grants.any? ? funder.grants.where('approved_on < ? AND approved_on >= ?', '2015-01-01', '2014-01-01').count : '...'
              %td ?
