.uk-container.uk-container-center
  .uk-width-medium-1-2.uk-float-left
    %h1= @funder.name
    %p= @recipient
    %h3 Grants awarded
    %ul
      - @grants.each do |grant|
        %li
          = link_to number_to_currency(grant.amount_awarded, unit: '£', precision: 0),
            edit_funder_grant_path(@funder, grant), class: 'uk-text-bold'
          for
          = link_to grant.recipient.name, recipient_path(grant.recipient)
      %li= link_to 'New Grant', new_funder_grant_path(@funder)
